mutation UpdateBot($id: ID!, $input: UpdateBotInput!) {
  updateBot(id: $id, input: $input) {
    ... on Bot {
      id
      name
      allocationPct
      dailyMaxLoss
      dailyMaxGain
      updatedAt
    }
    ... on ValidationError {
      message
      field
      code
    }
    ... on NotFoundError {
      message
    }
  }
}
