mutation CreateBot($input: CreateBotInput!) {
  createBot(input: $input) {
    ... on Bot {
      id
      name
      frame
      status
      allocationPct
    }
    ... on ValidationError {
      message
      field
      code
    }
  }
}
