mutation CreateBot($input: CreateBotInput!) {
  createBot(input: $input) {
    ... on Bot {
      id
      name
      frame
      status
      capitalAllocatedUsd
    }
    ... on ValidationError {
      code
      field
      message
    }
  }
}
