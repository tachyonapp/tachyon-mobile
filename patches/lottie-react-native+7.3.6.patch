diff --git a/node_modules/lottie-react-native/src/LottieView/index.tsx b/node_modules/lottie-react-native/src/LottieView/index.tsx
index 16c5927..e1c62b2 100644
--- a/node_modules/lottie-react-native/src/LottieView/index.tsx
+++ b/node_modules/lottie-react-native/src/LottieView/index.tsx
@@ -1,6 +1,7 @@
 import React from 'react';
 import {
   NativeSyntheticEvent,
+  type ProcessedColorValue,
   StyleProp,
   View,
   ViewStyle,
@@ -17,8 +18,7 @@ import NativeLottieAnimationView, {
 
 type Props = LottieViewProps & { containerStyle?: StyleProp<ViewStyle> };
 
-const defaultProps: Props = {
-  source: undefined,
+const defaultProps: Partial<Props> = {
   progress: 0,
   speed: 1,
   loop: true,
@@ -55,23 +55,35 @@ export class LottieView extends React.PureComponent<Props, {}> {
   }
 
   play(startFrame?: number, endFrame?: number): void {
-    Commands.play(
-      this.lottieAnimationViewRef,
-      startFrame ?? -1,
-      endFrame ?? -1,
-    );
+    const ref = this.lottieAnimationViewRef;
+    if (ref == null) {
+      return;
+    }
+    Commands.play(ref, startFrame ?? -1, endFrame ?? -1);
   }
 
   reset() {
-    Commands.reset(this.lottieAnimationViewRef);
+    const ref = this.lottieAnimationViewRef;
+    if (ref == null) {
+      return;
+    }
+    Commands.reset(ref);
   }
 
   pause() {
-    Commands.pause(this.lottieAnimationViewRef);
+    const ref = this.lottieAnimationViewRef;
+    if (ref == null) {
+      return;
+    }
+    Commands.pause(ref);
   }
 
   resume() {
-    Commands.resume(this.lottieAnimationViewRef);
+    const ref = this.lottieAnimationViewRef;
+    if (ref == null) {
+      return;
+    }
+    Commands.resume(ref);
   }
 
   private onAnimationFinish = (
@@ -117,16 +129,24 @@ export class LottieView extends React.PureComponent<Props, {}> {
     const sources = parsePossibleSources(source);
 
     const speed =
-      duration && sources.sourceJson && (source as any).fr
+      duration && sources?.sourceJson && (source as any).fr
         ? Math.round(
             (((source as any).op / (source as any).fr) * 1000) / duration,
           )
         : this.props.speed;
 
-    const colorFilters = this.props.colorFilters?.map((colorFilter) => ({
-      ...colorFilter,
-      color: processColor(colorFilter.color),
-    }));
+    const colorFilters = this.props.colorFilters
+      ?.map((colorFilter) => {
+        const color = processColor(colorFilter.color);
+        if (color == null) {
+          return null;
+        }
+        return { keypath: colorFilter.keypath, color };
+      })
+      .filter(
+        (item): item is { keypath: string; color: ProcessedColorValue } =>
+          item != null,
+      );
 
     return (
       <NativeLottieAnimationView
